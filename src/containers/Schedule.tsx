import useSchedule from "../hooks/useSchedule.ts";

function Schedule() {
	/*const {
		shifts,
		employees,
		loading,
		fromDate,
		employeeShiftMap,
		setFromDate,
		createShift,
		updateShift,
		deleteShift
	} = useSchedule();*/

return (
	<div>
		<h1>Schedule</h1>
	</div>
);
}

export default Schedule;